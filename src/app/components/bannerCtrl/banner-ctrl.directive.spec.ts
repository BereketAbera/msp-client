import { BannerCtrlDirective } from './banner-ctrl.directive';

describe('BannerCtrlDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerCtrlDirective();
    expect(directive).toBeTruthy();
  });
});
