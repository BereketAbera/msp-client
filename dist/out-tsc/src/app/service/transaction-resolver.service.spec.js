import { TestBed } from '@angular/core/testing';
import { TransactionResolverService } from './transaction-resolver.service';
describe('TransactionResolverService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(TransactionResolverService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=transaction-resolver.service.spec.js.map